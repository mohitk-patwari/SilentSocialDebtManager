{
  "name": "silent-social-debt-manager",
  "version": "1.0.0",
  "description": "Autonomous AI agent for forgotten follow-ups, unanswered messages, and broken relationship commitments",
  "private": true,
  "workspaces": [
    "packages/backend",
    "packages/frontend",
    "packages/shared"
  ],
  "scripts": {
    "dev": "yarn workspaces foreach run dev",
    "build": "yarn workspaces foreach run build",
    "test": "npm run test --workspaces --if-present",
    "lint": "yarn workspaces foreach run lint",
    "type-check": "yarn workspaces foreach run type-check"
  },
  "keywords": [
    "openClaw",
    "hackathon",
    "PRISM",
    "AI",
    "NLP",
    "scheduler"
  ],
  "author": "PRISM Hackathon Team",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14"
  }
}
